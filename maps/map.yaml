image: map_maze.png
resolution: 1
origin: [0.0, 0.0, 0.0] # x, y, theta，地图(0, 0)点在世界坐标系的位置
# origin: [2.0, 3.0, -0.35] # x, y, theta，地图(0, 0)点在世界坐标系的位置
occupied_thresh: 0.1 # 高于这个值认为是occupied
free_thresh: 0.05 # 低于这个值认为是free
negate: 0
